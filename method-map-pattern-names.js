//Name pattern. Letting all names with uppercase.

let heroesNames = ['wolverine', 'iron Man', 'Thor', 'spider Man', 'hulk']

const patternNames = heroesNames.map(name => name.toUpperCase())

console.log(patternNames)
